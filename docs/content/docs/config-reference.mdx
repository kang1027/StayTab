---
title: Config reference
description: Every config.json key — type, default, accepted range and allowed values.
icon: List
---

Every key accepted in `~/.config/bettercmdtab/config.json` and in exported settings
files. Keys are listed without the `Switcher.` prefix, which is how they are written.

All keys are optional. Omitting one keeps its current value — see
[partial edits](/configuration#partial-edits-and-error-handling). Numbers outside
the range shown are clamped rather than rejected, and an unrecognised string falls back
to the key's default.

The three array-of-objects keys are documented separately:
[`appExceptions`](/app-rules), [`scopedShortcutList` and
`shortcutOverrides`](/overrides).

<Callout type="info">
This page is generated from the same `schema.json` the app writes next to your config
file, so types, descriptions and allowed values are the app's own. Your editor shows
the identical information inline.
</Callout>

<ConfigReference />
